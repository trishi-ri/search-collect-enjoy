import { SearchTimerPipe } from './search-timer.pipe';

describe('SearchTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
